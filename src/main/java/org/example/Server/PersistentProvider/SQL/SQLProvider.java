package org.example.Server.PersistentProvider.SQL;

import org.example.Server.Entities.User;
import org.example.Server.PersistentProvider.PersistentProvider;

public class SQLProvider extends PersistentProvider {

    @Override
    public User getUser(String login, String password) {

    }
}
